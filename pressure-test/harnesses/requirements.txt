# FERA pressure-test harnesses — dependencies
# Verified working: Python 3.12.7, numpy 2.4.2, pandas 3.0.1 (macOS, 2026-07-10)
numpy>=1.24
# pandas is used only by the (optional) real-data loaders; harnesses run without it.
pandas>=2.0
