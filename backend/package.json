{
  "name": "@fera/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FERA indexer (Ponder), API, consensus-critical emissions pipeline, keepers, ops.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "ponder dev",
    "start": "ponder start",
    "serve": "ponder serve",
    "api:dev": "tsx api/devServer.ts",
    "codegen": "ponder codegen",
    "db": "ponder db",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "typecheck:ponder": "ponder codegen && tsc --noEmit -p tsconfig.ponder.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "pipeline:dryrun": "tsx pipeline/dryrun.ts",
    "reconcile": "tsx ops/reconcile.ts",
    "keeper:market-hours": "tsx keepers/marketHours.ts",
    "keeper:rwa-strategy": "tsx keepers/rwaStrategy.ts",
    "keeper:root-poster": "tsx keepers/emissionsRootPoster.ts",
    "keeper:oracle-monitor": "tsx keepers/oracleStaleness.ts",
    "keeper:event-calendar": "tsx keepers/eventCalendar.ts"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "ponder": "^0.9.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  },
  "overrides": {
    "@hono/node-server": "^1.19.14"
  }
}
