# docs/vault — Open Decisions / Change Requests (Agent V)

**Date:** 2026-07-11. Raised against `docs/VAULT_ARCHITECTURE.md` (D-12) and MASTER_SPEC
v0.4 per §12 protocol. Evidence: `ARCHITECTURE_REVIEW.md`, `COMPETITIVE_SCAN.md`,
`SIM_RESULTS.md`. Nothing here edits upstream docs; each row is a proposed amendment for
Orchestrator sign-off. Ordered by priority.

| # | What | Why (evidence) | Blast radius | Proposed change | Status |
|---|------|----------------|--------------|-----------------|--------|
| OD-V3 | **Replace D-13's hard JIT min-hold revert with a fee-forfeiture penalty** (OZ `LiquidityPenaltyHook` pattern: remove within `JIT_WINDOW` of last add forfeits `accruedFees × (1 − elapsed/window)`, donated to in-range LPs; principal always exits). | Review (d): at 100ms blocks a short hold doesn't deter (JIT EV −$8 at 0.1s on a $2,250 capture); a deterrent hold (~30–60 min) blocks withdrawals whenever a vault deposit/drip re-armed a band's clock → **contradicts INV-11**; fee-forfeit makes JIT EV ≤ 0 at any window and never blocks exits. | **Hook flag set changes** (needs `afterAddLiquidity`/`afterRemoveLiquidity` + both RETURN_DELTA flags) → CREATE2 **salt re-mine** (arithmetic in CHAIN.md §7 carries over); INV-1′ text; Contracts hook; Security D-13 attack plan; Mechanism freezes `JIT_WINDOW` (10–30 min range suggested). Fallback if flags must stay: liquidity-weighted hold timestamps + vault withdrawal routing away from in-window bands. | **OPEN — highest priority; breaks an invariant as adopted** |
| OD-V1 | **INV-5′ → INV-5″: add a guarded MEME principal recenter** (trigger: at-spot depth < `DEPTH_FLOOR_MULT`×v1-yardstick sustained `GUARD_PERSIST`; constraints: pool-TWAP sanity band, `MIN_RECENTER_INTERVAL_MEME`, max-slippage bound, randomized keeper timing; `StrategyAction kind=6`). Also: **drip deploys single-sided as a limit band (no swap)** — Charm pattern. | Review (b)/SIM: drip-only spends 70% of hours below v1 depth in a 3x run-up and captures less fee share than v1 (33% vs 43%); guarded variant holds 2.1–2.9x depth in all scenarios at 1–5 recenters/quarter with LP PnL within noise of drip in chop. Defaults sim-tested: floor 1.0x, persist 24h, interval 7d. | MASTER_SPEC §2 (INV-5), §6 (kind=6), Mechanism params, Contracts strategy module. Worst case (guard never fires) = current proposal. | **OPEN — answers the proposal's own §2.2 open question: drip-only is NOT enough** |
| OD-V2 | **Hard `MAX_BANDS_PER_TRANCHE` (6–8) + drip consolidation** (compound into an existing core-class band within ±X% of spot instead of minting new) + **define principal bands vs fee-funded bands** — fee-funded drip bands are recyclable under INV-5″'s own text ("only fee income may be (re)deployed"); principal bands stay untouchable. | Review (f)/SIM #4: never-recycle daily drip → 50–92 live bands per quarter → withdrawals are O(bands) ≈ 5–13M gas and unbounded; "≤ ~5 bands" header is fiction without this. | VAULT_ARCHITECTURE §2.1/§2.2 (`MEME_BAND_RECYCLE` default flips from "never" to "consolidate fee-bands only"), Contracts withdraw path, PARAMS.md keys. | **OPEN — required for the stated ≤5-band architecture to be real** |
| OD-V5 | **Deposit-guard hardening (Gamma-vector):** (1) spot-vs-TWAP deviation gate with **hardcoded immutable bounds** (timelocked inside them), never off-chain-configurable; (2) ratio-matched deposits + slippage-bounded swap-assist; (3) **fee checkpoint/collect before mint NAV** (v4 auto-credits accrued fees on any `modifyLiquidity` — without the checkpoint depositors buy pre-existing fees); (4) `DEPOSIT_COOLDOWN_SEC` (Veda share-lock precedent); (5) Bunni rounding-direction policy + **cumulative** dust bound per tranche, fuzz/PoC'd with the 44-micro-withdrawal pattern (R-17, INV-15). | Scan §1.2 (verified vector: threshold misconfigured to −50/+100% vs intended ~2% by config automation; ≥$4.5M), §1.8 (Bunni: per-op rounding dust compounded across 44 withdrawals). | Contracts deposit/withdraw paths, PARAMS.md (`DEPOSIT_COOLDOWN_SEC`, gate bounds, TWAP window — joint with PT-8), Security PoCs. | **OPEN — mostly confirms proposal §2.5; makes the guard parameter-robust** |
| OD-V4 | **MEME pools default single-tranche (Core ladder only); Anchor ships on RWA pools.** Type system keeps 2 per pool (proposal §2.3 already allows this). Gate MEME-Anchor on a Mechanism demand test: projected Anchor net APR > vanilla-30 (PT-3 analog). | Review (c): Anchor≈full-range on a memecoin is the product the principal rejected for LPs (sim: full-range cap_ratio 0.29 — direct LPs out-earn it 3.4x per $); Anchor's persona is the NVDA LP. | GTM copy, Frontend pool pages, Mechanism tranche-allocation params. No contract change (config). | OPEN |
| OD-V6 | **Tranche-scoped salt namespace** (`salt = bytes32(tranche, bandSeq)`) + invariant test that no two live bands share a v4 position key + drip bands inherit the funding tranche. | Review (c): v4 merges positions with identical (owner,ticks,salt) — identical-tick bands across tranches would silently merge fee accounting, breaking INV-15. Verified against v4 position keying + `IHooks` params (sender/ticks/salt available to hook). | Contracts FeraVault position bookkeeping; Security invariant suite. | OPEN — small, mandatory |
| OD-V7 | **Legal review of "tranche" framing** (Core/Anchor naming, no fixed-yield language, no waterfall claims). | Scan §1.11: SEC killed BarnBridge's senior/junior product as an unregistered security (Dec 2023, $1.7M penalties, ordered shutdown). FERA's disjoint-band share classes are materially different (no waterfall, no senior claim) but the framing is the attack surface. | GTM (7), R-8 owner (Orchestrator/legal). No code. | OPEN |
| OD-V8 | **Depth-claim wording:** state 4.1x as at-center/inception; steady-state depth is a recentering property (sim: 2.1–2.9x with guarded recenter; floor 0.30x if principal never moves and price leaves the mid band). | Review (a): numbers verified exact (4.106x weighted), but the honest steady-state figure is what routers actually see (PT-4 depth-is-load-bearing). | VAULT_ARCHITECTURE §2.1 wording; Frontend `depthVsBest` methodology. | OPEN — wording only |
| OD-V9 | **Surge-fee gap routed to Mechanism:** EWMA fee lags one-shot jumps — first dump of a calm pool trades near the 0.30% floor at max LP damage. Evaluate a jump-detector bump inside the frozen floor/ceiling (ceiling clamp already bounds it; no invariant change). Also: check JIT-guard interaction with RWA strategy ops (recenter/widen after fresh deposits) when sizing `JIT_WINDOW` per regime. | Scan §1.8 lesson 4 (Bunni surge fees); review (d)/(g). | Mechanism fee spec (MEME §), PARAMS.md; no ABI change. | OPEN |
| OD-V10 | **Emissions must bridge a measured 1.5–3x per-$ fee gap vs sophisticated direct LPs** (sim cap_ratio: drip 0.375 chop / 0.15 run-up; guarded 0.47–0.64): input to Decision-A′ split optimizer and INV-14's carrot sizing — if vault LP net yield (fees×0.9 + emissions) < direct-LP yield, the vault loses exactly the capital it needs (PT-4). | SIM_RESULTS #5. | Mechanism Decision-A′ derivation; GTM APR messaging. | OPEN |

**Verification asks (no decision needed):** Contracts CI to measure real multi-band
deposit/withdraw gas (review (f) estimates 350k–1M for 3–5 bands); Security to add
Cork-style hook access-control tests and the Bunni micro-withdrawal PoC (R-17).
