# FERA Mechanism — OPEN DECISIONS & interface-drift flags

Raised by Mechanism (Agent 1) for the Orchestrator. Per the build rules, Mechanism does **not**
edit MASTER_SPEC; it logs requests/flags here and proceeds against the current MASTER_SPEC. Each
row states: what, why, blast radius, and whether it needs Orchestrator/other-agent action.

**Bottom line: no hard MASTER_SPEC shared-interface change is required by the frozen mechanism
spec.** Everything below is either a confirmation, a cross-agent sign-off request, or a design
note. The event schema (§6), accounting constants (§7), and emissions pipeline (§9) all
accommodate the mechanism as written.

| # | Type | Item | Detail / requested action | Blast radius | Status |
|---|------|------|---------------------------|--------------|--------|
| D-M1 | Confirm (no change) | Per-pool caps + pool quality score are **off-chain, reproducible** | `MECHANISM_SPEC.md §4.4` computes per-pool caps and `divMult` from `Swap`/`FeesCollected` events. MASTER §9 already lists "pool quality scores, per-pool caps" as pipeline inputs, so no on-chain interface or event change is needed. Requesting Orchestrator confirm this reading. | Backend (pipeline), none on Contracts ABI | Open — confirm |
| D-M2 | Design note / risk | **Cold-start vs revenue-gated emissions tension** | Sims show base-case 4-yr emission ≈ 11% of the 900M bucket because `β·revenue` binds while TVL (hence revenue) is small. GTM logic (SHARED_CONTEXT §4) says "emissions solve the TVL cold-start." These reconcile only if the **10% genesis treasury war-chest** funds initial liquidity bootstrapping and the *usage* emissions are the steady-state dividend. Requesting Orchestrator confirm the bootstrap is a treasury/war-chest function, not the usage bucket, so Mechanism does not need to front-load `cap(t)` or raise β. | GTM (7), Treasury policy, token (2) | Open — confirm |
| D-M3 | Cross-agent sign-off | **Extreme-deviation circuit** (only allowed swap-revert, MASTER §5) | Defined in `MECHANISM_SPEC.md §2.4`, default **DISABLED** (`RWA_CIRCUIT_ENABLED=false`). Blocks only *deviation-increasing* swaps beyond `RWA_CIRCUIT_DEV_BPS` (10%) when a healthy oracle disagrees with spot. MASTER §5 requires Mechanism **+ Security** justification before enabling. Requesting Security (6) to model false-positive rate on real Chainlink stock feeds and co-sign before flipping on. Ships off in v1. | Security (6), Contracts (2) | Open — needs Security |
| D-M4 | Gas flag (measure) | **RWA Chainlink read may pressure the 40k budget** | `MECHANISM_SPEC.md §2.5`: a cold Chainlink proxy read can be 8–30k gas. Mitigation frozen: per-block per-pool **transient-storage oracle cache** (`RWA_ORACLE_TCACHE`) so only the first swap in a block pays it. Requesting Contracts (2) to measure the real proxy-read cost in CI; if a single cold read >25k, read the aggregator directly or use Chainlink Data Streams (live on the chain). Not an interface change. | Contracts (2), Deployment (5, feed addrs in CHAIN.md) | Open — measure |
| D-M5 | Confirm (no change) | Hook reads: `getSlot0` (post-swap tick) in afterSwap; Chainlink in beforeSwap (RWA) | Both are internal reads, not shared interfaces. `Swap` event `feeAmount`/`lpFeePips` already carry what Backend needs (MASTER §6). MASTER D-3 (feeAmount gas) is answered: the compact event fits the 40k budget (`§1.4`, ~3.1k for the LOG). | none | Resolved by mechanism |
| D-M6 | Provisional freeze | `[PROVISIONAL]` params gated on Pressure-Test V3/V4 | `MEME_FEE_SLOPE_PIPS_PER_TICK`, `MEME_FEE_SIGMA0_TICKS`, `MEME_VOL_LAMBDA_*`, `RWA_BAND_HALFWIDTH_BPS`, `RWA_STALE_AFTER_OPEN_SEC`. Sims accept real data via `sims/README.md` flags and re-emit frozen values. These MUST be frozen before Contracts parameter freeze (MASTER §11 gate). Requesting Pressure-Test (8) deliver V3 realized-tick histograms + swap-frequency and V4 RWA realized vol + feed heartbeats. | Pressure-Test (8) → Mechanism → Contracts (2) | Open — awaiting data |

## v2 rows (2026-07-11 — tokenomics re-derivation, MASTER v0.4 + vault-review deltas)

| # | Type | Item | Detail / requested action | Blast radius | Status |
|---|------|------|---------------------------|--------------|--------|
| D-M7 | Principal sign-off | **Emission split 80/10/10 (Decision-A′)** | `split_optimizer.py` + SPEC §7: objective monotone against s_TR in every scenario; s_TR=10% kept for GTM option value only (rebate is 0.24bp of volume — routing-irrelevant); s_TREAS=10% is a governance choice the model can't price. Marked FROZEN-PENDING-PRINCIPAL in PARAMS. On sign-off the Orchestrator must edit MASTER §7 (split row) **and §9 step "split 45/45/10"** — MASTER §9 text is now stale either way. | MASTER §7/§9, Backend pipeline, Frontend APR displays | **Open — needs principal** |
| D-M8 | §9 clarification (Orchestrator confirm) | **Pipeline order of operations** (INV-13 structural fix) | SPEC §4.4: per-pool revenue lock applied AFTER boost; split + boost normalized WITHIN each pool; LP-leaf weights are share-time-weighted, self-match-excluded vault fees. MASTER §9 currently reads as global-split-then-boost; requesting confirmation this ordering is the normative reading (it is what makes PT-2's boost theft impossible). | Backend §9 pipeline (consensus-critical), Contracts none | **Open — confirm; binds Backend** |
| D-M9 | Security co-sign | **LP self-deal honest residual (INV-13)** | `lp_self_deal.py` + SPEC §4.7: PT-2 boost leak eliminated; Rp≤1 net-negative ∀f; exclusion-caught ⇒ ∞ cost/profit; but the **absolute 10x bar is NOT met** when clustering is evaded and FERA appreciates (2.27x @Rp=2, 0.53x @Rp=4, full vest). Defensible bound = spot-purchase dominance (wash buys FERA at ≥+39% premium ∀Rp) + zero transfer from honest users + protocol revenue-positive. Requesting Security co-sign that "dominated, self-funded, honest-user-neutral" is an acceptable closure of PT-2/M4, or specify which residual lever to pull (longer LP vest / lower β / default-deny — each with stated costs). | Security (6), Pressure-Test M4 re-run | **Open — co-sign requested** |
| D-M10 | Cross-agent dependency | **Anti-JIT freeze is conditional on OD-V3/D-14** (fee-forfeiture) | Mechanism froze `JIT_PENALTY_WINDOW_SEC_MEME=1800` / `_RWA=600`, linear decay (SPEC §3.4.1) — all conditional on the Orchestrator-accepted flag-set change (afterAdd/afterRemove + return-delta) and salt re-mine. Patient-sniper residual at 30min ≈ +$189 on the reference $100k dump (vs −$1,250 at 60min) — accepted as paid-for liquidity; window timelocked upward if V3 shows extraction. Security to attack the forfeiture path (donation griefing, last-withdrawer edge, shared-periphery clock). | Contracts (hook flags + salt), CHAIN.md §7, Security | **Open — blocked on OD-V3 execution** |
| D-M11 | MASTER §10 addition (Orchestrator) | **Earnings-calendar keeper input (PT-7)** | New bounded keeper capability: flag a scheduled-event session ⇒ off-hours withdraw uses `RWA_EVENT_WITHDRAW_FRAC=0.80` (bounds `{q..0.9}`, current session only, fail-static). Needs a §10 keeper-table row + Backend keeper implementation. | MASTER §10, Backend keepers | **Open — request** |
| D-M12 | Param-bound change (record) | `RWA_OFFHOURS_WITHDRAW_FRAC` hard bound widened `[0,5000]`→`[0,8000]` bps | PT-7 freeze at q=0.60 exceeds the v1 bound. Mechanism-owned param; recorded so Contracts picks up the new bound with the value. | Contracts (bound constant) | **Recorded** |
| D-M13 | Flag (info) | **OD-V10 answered in the negative** | No emission split can make vault yield ≥ always-centered direct-LP yield per fee-dollar while `E ≤ β·perf·rev` (needs fee-capture ratio c ≥ 1.03; measured ≤ 0.64). Vault stickiness = passive/retail segmentation + guarded-recenter narrowing + cap-bound/bull epochs + war-chest. GTM must NOT market vault APR as dominating self-management. | GTM (7), principal expectation-setting | **Flagged** |
| D-M14 | Consistency note | **MEME single-tranche (D-16) ripple** | With MEME Core-only, the F-8 event-batch `tranche` field stays (RWA is dual-tranche); §8 `/pools` shapes unaffected. MEME-Anchor remains gated on the OD-V4 demand test (Anchor net APR > vanilla-30 — the PT-3 hurdle applied to the share class). | Backend (attribution), Frontend copy | **Recorded** |

## Notes appended for the record (no action needed)

- **β safety margin.** `EMISSION_BETA=0.8` keeps wash-farming net-negative for all `β < 1/0.9 =
  1.111` (`MECHANISM_SPEC.md §4.2`). If the Orchestrator ever raises β via timelock, keep it
  strictly `< 1.11` or the wash-farm break-even inverts. Recommend a hard on-chain upper bound
  of 9000 bps on the timelocked setter (already in `PARAMS.md#EMISSION_BETA`).
- **MEME strictly full-range.** `MECHANISM_SPEC.md §3.1` proves zero strategy risk; the only
  post-mint action allowed is `StrategyAction.kind=4` compound-in-place. Contracts should make
  any other `modifyLiquidity` on a MEME pool unreachable (INV-5).
