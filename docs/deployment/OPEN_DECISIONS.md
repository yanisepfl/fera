# FERA — Deployment (Agent 5) → Open Decisions / Change-Requests

Change-requests and flags raised by Deployment (5) for the Orchestrator to route/decide. Per MASTER_SPEC
§12, Deployment does not edit MASTER_SPEC directly; entries here are proposals/flags. Orchestrator
signs off (or rejects) and, on sign-off, edits the spec + the §13 Open-Decisions log.

| # | Date | Type | Item | Blast radius / route-to | Status |
|---|------|------|------|-------------------------|--------|
| DEP-1 | 2026-07-10 | FLAG | **Gas-fee holiday end date = ~2026-09-29** (90d from launch). Recorded in CHAIN.md §4. Closes R-10's "needs date". Also: the holiday almost certainly does **not** cover FERA keeper/contract txs — model keepers as paid from day one; treat user-side subsidy as an expiring tailwind. | Mechanism (1): size min pool size + rebalance frequency for the post-holiday cost regime. | Open — route to Mechanism |
| DEP-2 | 2026-07-10 | FLAG | **Native gas token is ETH** (authoritative RH docs), not a custom token. Keeper/strategy gas denominated in ETH; WETH is the canonical wrapped-gas/pair asset. | Mechanism (1) cost model; Contracts (2) fee/gas accounting. | Open — informational |
| DEP-3 | 2026-07-10 | FLAG | **FCFS sequencing, no priority-fee ordering, no Timeboost.** Lowers base front-run/sandwich risk but keep the RWA-keeper randomized-timing + private-RPC design (MASTER_SPEC §10, R-3). No spec change; confirms the existing mitigation is sufficient and not over-built. | Backend (4) keepers; Security (6). | Open — informational |
| DEP-4 | 2026-07-10 | FLAG (gate) | **All Uniswap v4 core addresses in CHAIN.md §2 are LIKELY, not on-chain-verified**, and PoolManager is not the usual cross-chain vanity address. Contracts (2) must not hardcode external immutables until CHAIN.md §8 B5–B8 are green. This tightens the MASTER_SPEC §3 note ("do not hardcode until CHAIN.md confirms"). | Contracts (2): keep `// TODO(chain-confirm)`; CI gate G5. | Open — blocks immutables freeze |
| DEP-5 | 2026-07-10 | QUESTION | **`0x91`-prefix avoidance for the hook address** is recorded as a project directive (CHAIN.md §7) but its technical rationale is UNVERIFIED. Confirm the reason and whether other prefixes are also unsafe, so the miner predicate is correct and minimal. | Security (6) + Orchestrator to confirm rationale. | Open — needs rationale |
| DEP-6 | 2026-07-10 | QUESTION | **Chainlink stock-feed decimals + heartbeats** (NVDA/AAPL/GOOG/ETH/USDG) are unknown until read on-chain (CHAIN.md §8 C11). Contracts' RWA oracle-adapter (INV-6, R-4) must not assume 18 or 8 decimals — parametrize per feed. | Contracts (2) + Mechanism (1). | Open — resolve via §8 checklist |
| DEP-7 | 2026-07-10 | FLAG | **v4 BUSL additional-use-grant** for third-party hooks (R-9) not yet read for this deployment. Blocker if it doesn't license third-party hooks (low probability). | Orchestrator (legal) + Deployment (5) §8 B10. | Open — documentation gate |
| DEP-8 | 2026-07-12 | FLAG (DEPLOY-1 resolution) | **The {EsFera↔Distributor↔EmissionsController} + {AnchorStaking↔RevenueDistributor} + {Vault↔Hook} cycles are all mutual *constructor-arg* cycles with `immutable` peers.** Pure CREATE2 CANNOT close such a cycle (each address is a fixed point of the other's init-code hash). Resolved WITHOUT touching `src/` via **nonce-based CREATE precompute from a dedicated deployer** (see `DEPLOY_ORDER.md` §1–§2 + `script/Deploy.s.sol`). Only the hook truly needs CREATE2 (flag mining `0x25C3`), mined against the precomputed Vault address. **Correction:** `EsFera.minter` is the **Distributor**, not the EmissionsController — the SCC is real but the inner 2-cycle is EsFera↔Distributor. | Contracts (2) informational; Orchestrator to note. | Open — informational (works with code as-is) |
| DEP-9 | 2026-07-12 | QUESTION | **Optional pure-CREATE2 determinism.** If cross-chain (testnet==mainnet) address parity is ever wanted, Contracts would add a one-shot setter to break ONE edge per cycle (exactly the pattern `FeraToken.setEmissionsController` already uses) OR we CREATE2-deploy a `FeraDeployer` that uses internal CREATE. Not needed for v1 launch (CHAIN.md §7 allows different testnet/mainnet salts). | Contracts (2) if pursued; else no-op. | Open — not required for launch |
| DEP-10 | 2026-07-12 | FLAG (config) | **`AnchorStaking.addRewardToken` (onlyRewardTokenAdmin=timelock) and `FeraVault.createPool` (onlyKeeper) are post-deploy role-gated steps** the deployer EOA cannot perform — they are Safe txs (48h for addRewardToken). Revenue must not route to RevenueDistributor until FERA + each revenue token is allowlisted (SEC-6/R-21 crowd-out/poison defense). Sequenced in `DEPLOY_ORDER.md` §4. | Orchestrator to sequence with launch; Backend (F-10/F-11 keeper updates). | Open — launch sequencing |
| DEP-11 | 2026-07-12 | FLAG (CI) | **Gas-budget assert wired** in root `.github/workflows/contracts.yml` — parses `forge --gas-report` and fails if `beforeSwap+afterSwap` median > 40k (GAS-1; measured ~15.1k). Root `.github/workflows/` now holds contracts/frontend/backend/mainnet-deploy; the pre-existing `contracts/.github/workflows/contracts.yml` is superseded (kept for now, Contracts may delete). | Contracts (2) to dedupe the two contracts.yml. | Open — housekeeping |

*Append-only. Deployment (5) adds rows; Orchestrator resolves.*
